📅 2026-07-24

I WAY -> scapy
#!/usr/bin/env python3
from scapy.all import *
packet = IP(dst="10.0.0.2", proto=0xFF)
send(packet)

II WAY -> socket
#!/usr/bin/env python3
import socket
import struct
s = socket.socket(socket.AF_INET, socket.SOCK_RAW, socket.IPPROTO_RAW)
s.setsockopt(socket.IPPROTO_IP, socket.IP_HDRINCL, 1)
version = 4
ihl = 5
ver_ihl = (version << 4) | ihl
tos = 0
tot_len = 20
ident = 1234
frag = 0
ttl = 64
proto = 0xFF
check = 0
src = socket.inet_aton("10.0.0.1")
dst = socket.inet_aton("10.0.0.2")
ip_header = struct.pack(
    "!BBHHHBBH4s4s",
    ver_ihl,
    tos,
    tot_len,
    ident,
    frag,
    ttl,
    proto,
    check,
    src,
    dst,
)
s.sendto(ip_header, ("10.0.0.2", 0))

## 🧑🏾‍🏫 what this challenge teaches u
how to send an IP packet with scapy or socket

## 🏁 Flag  
pwn.college{MhqVynMZolFEHdmdYOmk6uzFudg.QX3YzMzwCO4IDOxEzW}
