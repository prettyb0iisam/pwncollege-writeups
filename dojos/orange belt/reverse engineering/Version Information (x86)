📅 2026-04-13

opened Binary Ninja with the file cimg
file header size = 12
magic number (hex) = 5b 4d  36 45
version (hex) = 7e
touch bro.cimg
vim bro:
#!/usr/bin/env python3
import struct
with open("bro.cimg", "wb") as bro:
      bro.write(b"[M6E")
      bro.write(struct.pack("<I", 231))
      bro.write(b"\x00\x00\x00\x00")

chmod a+x bro
./bro
/challenge/cimg bro.cimg
flag popped out

## 🧑🏾‍🏫 what this challenge teaches u
how to writes null bytes in file's version
u cannot use struct.pack, instead writes them manually
since /challenge/cimg checks for file's header size, u have to fill the remaining bytes

## 🏁 Flag  
pwn.college{Y0bbEfg-kyFTgt3z_FdkcSd4ixS.0lMwMDMxwCO4IDOxEzW}
